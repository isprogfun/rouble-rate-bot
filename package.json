{
  "name": "rouble-rate-bot",
  "author": "Ivan Shcherbakov",
  "license": "MIT",
  "engines": {
    "node": ">=5.0.0"
  },
  "dependencies": {
    "express": "^4.16.3",
    "mongodb": "^3.0.10"
  },
  "devDependencies": {
    "@types/express": "^4.16.0",
    "@types/mongodb": "^3.0.19",
    "@types/node": "^10.3.1",
    "eslint": "^4.19.1",
    "eslint-config-airbnb-base": "^12.1.0",
    "eslint-plugin-import": "^2.12.0",
    "pm2": "^2.10.4",
    "typescript": "^2.9.1"
  },
  "scripts": {
    "setup": "pm2 deploy ecosystem.config.js production setup",
    "deploy": "pm2 deploy ecosystem.config.js production",
    "dump": "mongodump -d rouble-rate-bot && tar -czvf dump.tar.gz dump && aws s3 cp dump.tar.gz s3://isprogfun/rouble-rate-bot/db/",
    "cron": "make cron",
    "build": "tsc"
  }
}
